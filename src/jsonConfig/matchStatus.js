// 匹配后台返回的status对应的值
/**
 * 
 * @param {Array} tar 传入值
 * @param {Json} jsonData 匹配范围
 * @param {String} Key 分类字段
 */
function matchStatus(tar, jsonData,key) {
    tar.map((v,i) => {
        jsonData[v]
    })
}